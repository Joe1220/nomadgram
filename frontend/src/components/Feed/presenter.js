import React from "react";
import propTypes from "prop-types";
import styles from "./styles.scss";

const Feed = (props, context) => "Feed";

Feed.propTypes = {
  loading: propTypes.bool.isRequired
}

export default Feed;